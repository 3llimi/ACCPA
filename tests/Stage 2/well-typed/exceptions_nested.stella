language core;
extend with #exceptions, #exception-type-declaration;
exception type = Nat
fn main(n : Nat) -> Nat {
  return try {
    try { throw(succ(0)) } catch { e => throw(succ(e)) }
  } catch {
    e => e
  }
}