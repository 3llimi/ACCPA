language core;
extend with #exceptions, #exception-type-declaration;
exception type = Nat
fn handler(e : Nat) -> Nat {
  return 0
}
fn risky(n : Nat) -> Nat {
  return if Nat::iszero(n) then throw(n) else n
}
fn main(n : Nat) -> Nat {
  return try { risky(n) } with { handler }
}