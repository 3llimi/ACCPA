language core;
extend with #unit-type, #references, #let-bindings, #sequencing;
fn inc(r : &Nat) -> Unit {
  return r := succ(*r)
}
fn main(n : Nat) -> Nat {
  return let r = new(n) in
    inc(r);
    inc(r);
    inc(r);
    *r
}