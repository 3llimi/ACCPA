language core;
extend with #unit-type, #references, #let-bindings, #sequencing;
fn double_inc(r : &Nat) -> Unit {
  return r := succ(succ(*r))
}
fn main(n : Nat) -> Nat {
  return let r = new(n) in
    double_inc(r);
    *r
}